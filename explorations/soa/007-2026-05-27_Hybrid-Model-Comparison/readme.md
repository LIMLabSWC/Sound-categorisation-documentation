# In mice:

- sigma_noise: in BE fitted models are smaller than in SC fitted models.

- A_repulsion: in BE fitted models are larger than in SC fitted models.

- eta_learning : in BE fitted models are small, but way larger in SC fitted models.

- gamma : in BE fitted models are smaller than in SC fitted models.

- sigma_boundary: Not big difference in BE/SC fitted models, but difference between TPE/CMA-ES methods for each subject.

- sigma_upadte: in BE fitted models are smaller than in SC fitted models.

- training error: Not a big difference in range. sometimes BE is better, sometimes SC.


